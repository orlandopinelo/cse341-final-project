### Ignore the env
.env
### Ignore the node_modules due to size, however if you need the packages
### just type in the terminal "npm install" and it will install every package
### found in the package.json file.
node_modules